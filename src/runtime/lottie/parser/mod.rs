mod animatable_path_value_parser;
use animatable_path_value_parser::*;

mod animatable_text_properties_parser;
use animatable_text_properties_parser::*;

mod animatable_transform_parser;
use animatable_transform_parser::*;

mod animatable_value_parser;
use animatable_value_parser::*;

mod circle_shape_parser;
use circle_shape_parser::*;

mod color_parser;
use color_parser::*;

mod content_model_parser;
use content_model_parser::*;

mod document_data_parser;
use document_data_parser::*;

mod float_parser;
use float_parser::*;

mod gradient_color_parser;
use gradient_color_parser::*;

mod gradient_fill_parser;
use gradient_fill_parser::*;

mod gradient_stroke_parser;
use gradient_stroke_parser::*;

mod integer_parser;
use integer_parser::*;

mod json_utils;
use json_utils::*;

mod keyframe_parser;
use keyframe_parser::*;

mod keyframes_parser;
use keyframes_parser::*;

mod layer_parser;
use layer_parser::*;

mod lottie_composition_parser;
use lottie_composition_parser::*;

mod mask_parser;
use mask_parser::*;

mod merge_paths_parser;
use merge_paths_parser::*;

mod offset_parser;
use offset_parser::*;

mod path_keyframe_parser;
use path_keyframe_parser::*;

mod path_parser;
use path_parser::*;

mod polysar_shape_parser;
use polysar_shape_parser::*;

mod rectangle_shape_parser;
use rectangle_shape_parser::*;

mod repeat_parser;
use repeat_parser::*;

mod scale_xy_parser;
use scale_xy_parser::*;

mod shape_data_parser;
use shape_data_parser::*;

mod shape_fill_parser;
use shape_fill_parser::*;

mod shape_group_parser;
use shape_group_parser::*;

mod shape_path_parser;
use shape_path_parser::*;

mod shape_stroke_parser;
use shape_stroke_parser::*;

mod shape_trim_path_parser;
use shape_trim_path_parser::*;

mod value_parser;
use value_parser::*;